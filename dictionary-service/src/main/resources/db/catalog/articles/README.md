# Catalog Articles

These Markdown files are seed data for `V104__import_catalog_articles`.

Each `## Name` section is matched by name to an existing row in the dictionary
tables and copied into the `description` column during Flyway migration.
